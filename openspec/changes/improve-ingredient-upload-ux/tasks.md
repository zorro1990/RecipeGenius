## 1. 体验梳理与校准
- [x] 1.1 对照当前食材上传流程录屏，列出上传前/识别后/确认后需要保留的核心信息。
- [x] 1.2 与产品确认分类建议数据源（固定清单或来自 AI），明确补充流程的内容策略。

## 2. 设计与对齐
- [x] 2.1 完成新的识别结果布局草图（含折叠描述、分类筛选、建议清单交互），并与团队过稿。
- [x] 2.2 更新 CTA 状态图，覆盖加载、成功、失败三种状态及按钮文案。

## 3. 实现
- [x] 3.1 重构上传面板与提示区，折叠功能说明并强化上传指引。
- [x] 3.2 调整识别结果组件：默认折叠文本描述、展示可编辑列表、展示置信度与编辑入口。
- [x] 3.3 为分类标签增加筛选/补充逻辑，支持从建议清单中添加食材并同步状态。（后续 UX 评审决定移除分类板块）
- [x] 3.4 合并 CTA 状态流，移除重复按钮并实现加载态、完成态的视觉反馈。

## 4. 验证
- [x] 4.1 进行桌面与移动端手动测试，覆盖上传失败、识别成功、有遗漏补充、生成菜谱中断等场景。
- [x] 4.2 运行 `npm run build:check` 与 `openspec validate improve-ingredient-upload-ux --strict`。
