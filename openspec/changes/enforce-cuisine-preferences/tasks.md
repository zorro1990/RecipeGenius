## 1. 研究与规划
- [x] 1.1 梳理现有 `DIETARY_RESTRICTIONS`、菜系选项及提示词逻辑，列出需要覆盖的菜系特征与常见调味词。
- [x] 1.2 评估生成 API 的重试机制，明确新增校验失败时的重试上限与惩罚策略。

## 2. 规范与数据准备
- [x] 2.1 编写菜系特征配置（风味标签、核心食材、典型烹饪方式），落地到 `lib/cuisine-profiles.ts`（或同等模块）。
- [x] 2.2 更新 OpenSpec `recipe-generation` 规范，描述菜系偏好必须被硬性校验的行为。

## 3. 实现
- [x] 3.1 扩展提示词拼装逻辑，将菜系特征转化为结构化约束（含禁止示例与建议做法）。
- [x] 3.2 在 `generateRecipe` 完成后新增菜系匹配校验和带惩罚的重试流程，并将匹配结果写入菜谱对象。
- [x] 3.3 更新前端页面，展示菜系匹配信息和重试提示，确保失败场景有用户反馈。

## 4. 验证
- [ ] 4.1 使用至少三种不同菜系、各两组食材进行手动回归，记录匹配结果。
- [ ] 4.2 运行 `npm run build:check` 与 `openspec validate enforce-cuisine-preferences --strict`。
